# 数据库重建分段执行指南

## 📋 概述

由于完整的数据库重建脚本较长，可能会遇到各种错误。本指南将重建过程分为 12 个独立的小脚本，每个脚本都可以单独执行，便于调试和修复问题。

## ⚠️ 重要提示

1. **按顺序执行**：脚本需要按照编号顺序执行（01 → 02 → 03 ... → 12）
2. **每个脚本独立**：每个脚本都可以单独执行，但如果前面的脚本失败，后面的脚本可能会失败
3. **幂等性**：每个脚本都是幂等的，可以安全地多次执行
4. **检查错误**：执行每个脚本后，检查是否有错误，修复后再继续

## 🚀 执行步骤

### 步骤 1: 删除所有旧表和视图

**文件**: `rebuild_01_drop_tables.sql`

```sql
-- 执行此脚本会删除所有业务表和视图
-- 注意：不会删除 auth.users 表（Supabase 管理）
```

**执行后检查**:
- 确认所有旧表已删除
- 确认所有视图已删除
- 确认所有函数已删除

---

### 步骤 2: 创建核心函数

**文件**: `rebuild_02_create_functions.sql`

```sql
-- 创建所有核心函数
-- 包括：更新时间触发器函数、数据验证函数等
```

**执行后检查**:
- 确认所有函数已创建
- 检查是否有语法错误

---

### 步骤 3: 创建 users 表

**文件**: `rebuild_03_create_users_table.sql`

```sql
-- 创建 users 表
-- 包括：表结构、索引、触发器、唯一约束
```

**执行后检查**:
- 确认 users 表已创建
- 确认唯一约束 `users_email_role_unique` 已创建
- 确认所有索引已创建

**常见问题**:
- 如果出现约束已存在的错误，脚本会自动处理

---

### 步骤 4: 创建商家相关表

**文件**: `rebuild_04_create_merchants.sql`

```sql
-- 创建商家表、商家成员表、管理员邀请码表
```

**执行后检查**:
- 确认 merchants 表已创建
- 确认 merchant_members 表已创建
- 确认 admin_invite_codes 表已创建

---

### 步骤 5: 创建活动相关表

**文件**: `rebuild_05_create_events.sql`

```sql
-- 创建活动表、价格表
-- 包括：数据验证触发器
```

**执行后检查**:
- 确认 events 表已创建
- 确认 prices 表已创建
- 确认数据验证触发器已创建

---

### 步骤 6: 创建订单和票据表

**文件**: `rebuild_06_create_orders_tickets.sql`

```sql
-- 创建订单表、票据表
```

**执行后检查**:
- 确认 orders 表已创建
- 确认 tickets 表已创建
- 确认所有索引已创建

---

### 步骤 7: 创建其他表

**文件**: `rebuild_07_create_other_tables.sql`

```sql
-- 创建活动内容表、联系消息表
```

**执行后检查**:
- 确认 activities 表已创建
- 确认 contact_messages 表已创建

---

### 步骤 8: 创建用户同步触发器

**文件**: `rebuild_08_create_user_sync_trigger.sql`

```sql
-- 创建用户同步触发器
-- 自动将 auth.users 同步到 public.users
-- 回填现有的 auth.users 数据
```

**执行后检查**:
- 确认触发器函数已创建
- 确认触发器已创建
- 确认现有数据已回填

**验证查询**:
```sql
-- 检查数据同步
SELECT 
  'auth.users' as table_name,
  COUNT(*) as count
FROM auth.users
UNION ALL
SELECT 
  'public.users' as table_name,
  COUNT(*) as count
FROM public.users;
```

---

### 步骤 9: 创建视图

**文件**: `rebuild_09_create_views.sql`

```sql
-- 创建活动概览视图、商家统计视图
```

**执行后检查**:
- 确认 events_overview 视图已创建
- 确认 merchant_stats 视图已创建

---

### 步骤 10: 启用 RLS

**文件**: `rebuild_10_enable_rls.sql`

```sql
-- 启用所有表的 RLS
```

**执行后检查**:
- 确认所有表的 RLS 已启用

---

### 步骤 11: 创建 RLS 策略

**文件**: `rebuild_11_create_rls_policies.sql`

```sql
-- 创建所有表的 RLS 策略
-- 包括：用户、商家、活动、订单、票据等
```

**执行后检查**:
- 确认所有 RLS 策略已创建
- 检查策略是否正确

**验证查询**:
```sql
-- 检查 RLS 策略
SELECT schemaname, tablename, policyname, permissive, roles, cmd
FROM pg_policies
WHERE schemaname = 'public'
ORDER BY tablename, policyname;
```

---

### 步骤 12: 设置权限

**文件**: `rebuild_12_set_permissions.sql`

```sql
-- 设置服务角色、认证用户、匿名用户的权限
```

**执行后检查**:
- 确认所有权限已设置
- 确认数据库重建完成

---

## 📊 执行顺序总结

```
1. rebuild_01_drop_tables.sql          → 删除旧表和视图
2. rebuild_02_create_functions.sql     → 创建核心函数
3. rebuild_03_create_users_table.sql   → 创建 users 表
4. rebuild_04_create_merchants.sql     → 创建商家相关表
5. rebuild_05_create_events.sql        → 创建活动相关表
6. rebuild_06_create_orders_tickets.sql → 创建订单和票据表
7. rebuild_07_create_other_tables.sql  → 创建其他表
8. rebuild_08_create_user_sync_trigger.sql → 创建用户同步触发器
9. rebuild_09_create_views.sql         → 创建视图
10. rebuild_10_enable_rls.sql          → 启用 RLS
11. rebuild_11_create_rls_policies.sql → 创建 RLS 策略
12. rebuild_12_set_permissions.sql     → 设置权限
```

## ✅ 验证清单

执行完所有脚本后，请验证以下项目：

- [ ] 所有表已创建
- [ ] 所有索引已创建
- [ ] 所有触发器已创建
- [ ] 所有 RLS 策略已设置
- [ ] 用户同步触发器正常工作
- [ ] 数据已从 auth.users 同步到 public.users
- [ ] 所有视图已创建
- [ ] 所有权限已设置

## 🔍 验证查询

执行以下查询验证数据库结构：

```sql
-- 检查所有表
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;

-- 检查 users 表结构
SELECT column_name, data_type, is_nullable
FROM information_schema.columns
WHERE table_schema = 'public'
AND table_name = 'users'
ORDER BY ordinal_position;

-- 检查数据同步
SELECT 
  'auth.users' as table_name,
  COUNT(*) as count
FROM auth.users
UNION ALL
SELECT 
  'public.users' as table_name,
  COUNT(*) as count
FROM public.users;

-- 检查 RLS 策略
SELECT schemaname, tablename, policyname
FROM pg_policies
WHERE schemaname = 'public'
ORDER BY tablename, policyname;

-- 检查触发器
SELECT trigger_name, event_manipulation, event_object_table
FROM information_schema.triggers
WHERE event_object_schema = 'auth'
AND event_object_table = 'users';
```

## 🐛 常见问题

### Q: 执行脚本时出现约束已存在的错误？

A: 脚本已经处理了这个问题，但如果仍然出现，可以手动删除约束：

```sql
ALTER TABLE public.users DROP CONSTRAINT IF EXISTS users_email_role_unique CASCADE;
```

### Q: 触发器创建失败？

A: 检查函数是否已创建：

```sql
SELECT routine_name, routine_type
FROM information_schema.routines
WHERE routine_schema = 'public'
AND routine_name LIKE '%user%';
```

### Q: RLS 策略创建失败？

A: 检查表是否存在，RLS 是否已启用：

```sql
SELECT tablename, rowsecurity
FROM pg_tables
WHERE schemaname = 'public';
```

### Q: 数据没有同步到 public.users？

A: 检查触发器是否已创建：

```sql
SELECT trigger_name, event_manipulation, event_object_table
FROM information_schema.triggers
WHERE event_object_schema = 'auth'
AND event_object_table = 'users';
```

如果触发器不存在，重新执行步骤 8。

## 📝 注意事项

1. **备份数据**：在执行重建之前，确保已备份重要数据
2. **测试环境**：建议先在测试环境执行，确认无误后再在生产环境执行
3. **逐步执行**：不要一次性执行所有脚本，逐步执行并检查错误
4. **错误处理**：如果某个脚本执行失败，修复错误后重新执行该脚本
5. **验证功能**：执行完所有脚本后，测试所有功能确保正常

## 🎉 完成

如果所有脚本都成功执行，并且验证查询都通过，恭喜！数据库重建完成，你现在有一个干净的、功能完整的数据库。

如果遇到任何问题，请查看错误日志或联系技术支持。

