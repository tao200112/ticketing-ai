# 数据库重建完成检查清单

## 📊 当前状态

根据你的截图，数据库重建已经基本完成！以下是检查清单：

### ✅ 已完成的项

- [x] **users 表已创建** - 结构完整，包含所有必需字段
- [x] **所有核心表已创建** - activities, admin_invite_codes, contact_messages, events, merchant_members, merchants, orders, prices, tickets
- [x] **视图已创建** - events_overview, merchant_stats
- [x] **users 表约束正确** - 主键、外键、唯一约束、检查约束都已设置
- [x] **users 表有数据** - 已有 2 条记录（说明用户同步触发器可能已工作）

### ⚠️ 需要清理的项

- [ ] **profiles 表** - 旧用户表，可以删除
- [ ] **users_legacy_backup 表** - 用户备份表，可以删除
- [ ] **event_prices 表** - 旧价格表（如果存在），需要确认是否与 prices 表重复

## 🧹 清理步骤

### 步骤 1: 执行清理脚本

执行 `cleanup_old_tables.sql` 脚本清理旧表：

```sql
-- 在 Supabase SQL Editor 中执行
-- 文件: supabase/migrations/cleanup_old_tables.sql
```

这个脚本会：
- 删除 `profiles` 表（如果存在）
- 删除 `users_legacy_backup` 表（如果存在）
- 检查并删除 `event_prices` 表（如果存在且为空）

### 步骤 2: 执行最终验证

执行 `final_verification.sql` 脚本验证所有设置：

```sql
-- 在 Supabase SQL Editor 中执行
-- 文件: supabase/migrations/final_verification.sql
```

这个脚本会检查：
- 所有表是否存在
- 所有视图是否存在
- 所有约束是否正确
- 所有索引是否创建
- 所有触发器是否设置
- RLS 是否启用
- RLS 策略是否正确
- 数据是否同步
- 函数是否正确

## ✅ 验证清单

执行验证脚本后，检查以下项目：

### 表检查
- [ ] users 表存在且结构正确
- [ ] merchants 表存在
- [ ] merchant_members 表存在
- [ ] admin_invite_codes 表存在
- [ ] events 表存在
- [ ] prices 表存在
- [ ] orders 表存在
- [ ] tickets 表存在
- [ ] activities 表存在
- [ ] contact_messages 表存在

### 视图检查
- [ ] events_overview 视图存在
- [ ] merchant_stats 视图存在

### 约束检查
- [ ] users 表主键约束存在
- [ ] users 表外键约束存在（引用 auth.users）
- [ ] users 表唯一约束存在（email, role）
- [ ] users 表检查约束存在（role, auth_provider）

### 索引检查
- [ ] 所有表的索引已创建
- [ ] users 表索引完整

### 触发器检查
- [ ] 用户同步触发器已创建（on_auth_user_created_to_users）
- [ ] 更新时间触发器已创建
- [ ] 数据验证触发器已创建

### RLS 检查
- [ ] 所有表的 RLS 已启用
- [ ] 所有表的 RLS 策略已创建

### 数据检查
- [ ] auth.users 和 public.users 数据已同步
- [ ] users 表中有数据（如果有 auth.users 数据）

### 函数检查
- [ ] update_updated_at_column 函数存在
- [ ] update_activities_updated_at 函数存在
- [ ] check_inventory_constraint 函数存在
- [ ] check_event_attendees_constraint 函数存在
- [ ] handle_new_auth_user_to_users 函数存在

## 🔍 快速验证查询

### 检查所有表
```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_type = 'BASE TABLE'
ORDER BY table_name;
```

### 检查 users 表数据
```sql
SELECT id, email, name, role, auth_provider, created_at
FROM public.users
ORDER BY created_at DESC
LIMIT 10;
```

### 检查数据同步
```sql
SELECT 
    'auth.users' as table_name,
    COUNT(*) as count
FROM auth.users
UNION ALL
SELECT 
    'public.users' as table_name,
    COUNT(*) as count
FROM public.users;
```

### 检查 RLS 状态
```sql
SELECT 
    tablename,
    CASE WHEN rowsecurity THEN '✅ 已启用' ELSE '❌ 未启用' END as rls_status
FROM pg_tables
WHERE schemaname = 'public'
ORDER BY tablename;
```

### 检查用户同步触发器
```sql
SELECT 
    trigger_name,
    event_object_table,
    action_timing
FROM information_schema.triggers
WHERE event_object_schema = 'auth'
AND event_object_table = 'users'
AND trigger_name = 'on_auth_user_created_to_users';
```

## 🚀 后续步骤

### 1. 清理旧表

执行清理脚本删除不需要的旧表：
- `profiles`
- `users_legacy_backup`
- `event_prices`（如果存在且不需要）

### 2. 测试功能

测试以下功能确保一切正常：

- [ ] **用户注册** - 测试邮箱注册
- [ ] **Google OAuth 登录** - 测试 Google 登录
- [ ] **商家创建** - 测试商家账户创建
- [ ] **活动创建** - 测试活动创建
- [ ] **订单创建** - 测试订单创建
- [ ] **票据生成** - 测试票据生成

### 3. 检查错误日志

检查应用错误日志，确保没有数据库相关的错误：
- 检查 Vercel 日志
- 检查 Supabase 日志
- 检查浏览器控制台

## 📝 注意事项

1. **备份数据** - 在删除旧表之前，确保已备份重要数据
2. **测试环境** - 建议先在测试环境执行清理脚本
3. **逐步执行** - 不要一次性删除所有旧表，逐步执行并检查
4. **验证功能** - 清理后测试所有功能确保正常

## 🎉 完成

如果所有验证项目都通过，恭喜！数据库重建完成，你现在有一个干净的、功能完整的数据库。

如果遇到任何问题，请查看错误日志或联系技术支持。

