# 数据库重建指南

## 📋 概述

本指南将帮助你安全地删除所有旧数据并重新构建一个干净的数据库，确保所有功能正常工作。

## ⚠️ 重要警告

**在执行此操作之前，请确保：**

1. ✅ **已备份所有重要数据**（如果需要）
2. ✅ **已通知团队成员**（如果有多人协作）
3. ✅ **已准备好重新创建测试数据**
4. ✅ **了解此操作不可逆**

## 🎯 执行步骤

### 步骤 1: 登录 Supabase Dashboard

1. 打开 [Supabase Dashboard](https://app.supabase.com/)
2. 选择你的项目
3. 进入 **SQL Editor**

### 步骤 2: 执行重建脚本

1. 打开文件：`supabase/migrations/20251111_complete_database_rebuild.sql`
2. 复制整个脚本内容
3. 在 Supabase SQL Editor 中粘贴
4. 点击 **Run** 执行脚本

### 步骤 3: 验证重建结果

执行以下查询验证数据库结构：

```sql
-- 检查所有表是否存在
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
ORDER BY table_name;

-- 检查 users 表结构
SELECT column_name, data_type, is_nullable
FROM information_schema.columns
WHERE table_schema = 'public'
AND table_name = 'users'
ORDER BY ordinal_position;

-- 检查数据同步（应该显示 auth.users 中的数据已同步到 public.users）
SELECT 
  'auth.users' as table_name,
  COUNT(*) as count
FROM auth.users
UNION ALL
SELECT 
  'public.users' as table_name,
  COUNT(*) as count
FROM public.users;
```

### 步骤 4: 测试功能

1. **测试用户注册**
   - 访问注册页面
   - 创建一个新用户
   - 验证用户已创建

2. **测试 Google OAuth 登录**
   - 点击 Google 登录按钮
   - 完成 OAuth 授权
   - 验证登录成功

3. **测试商家功能**
   - 创建一个商家账户
   - 验证商家已创建
   - 测试商家登录

4. **测试活动创建**
   - 创建一个活动
   - 验证活动已创建
   - 测试活动发布

5. **测试订单和票据**
   - 创建一个测试订单
   - 验证票据已生成
   - 测试票据验证

## 📊 数据库结构

重建后的数据库包含以下表：

### 核心表

1. **users** - 用户表（与 auth.users 同步）
2. **merchants** - 商家表
3. **merchant_members** - 商家成员表
4. **admin_invite_codes** - 管理员邀请码表

### 业务表

5. **events** - 活动表
6. **prices** - 价格表
7. **orders** - 订单表
8. **tickets** - 票据表

### 内容表

9. **activities** - 活动内容表
10. **contact_messages** - 联系消息表

## 🔒 安全特性

### RLS (Row Level Security)

所有表都已启用 RLS，并设置了适当的安全策略：

- **Users**: 用户可以查看和更新自己的资料
- **Merchants**: 公开可以查看已验证的商家，商家拥有者可以管理自己的商家
- **Events**: 公开可以查看已发布的活动，商家成员可以管理自己的活动
- **Orders**: 用户可以查看自己的订单，商家可以查看自己商家的订单
- **Tickets**: 用户可以查看自己的票据，商家可以查看和更新自己商家的票据

### 数据完整性

- 外键约束确保数据一致性
- 检查约束确保数据有效性
- 触发器自动同步 auth.users 到 public.users
- 触发器自动更新 updated_at 字段

## 🔧 触发器

### 自动同步触发器

- **on_auth_user_created_to_users**: 自动将 auth.users 中的新用户同步到 public.users

### 更新时间触发器

- **update_users_updated_at**: 自动更新 users.updated_at
- **update_merchants_updated_at**: 自动更新 merchants.updated_at
- **update_events_updated_at**: 自动更新 events.updated_at
- **update_prices_updated_at**: 自动更新 prices.updated_at
- **update_orders_updated_at**: 自动更新 orders.updated_at
- **update_activities_updated_at**: 自动更新 activities.updated_at
- **update_contact_messages_updated_at**: 自动更新 contact_messages.updated_at

### 数据验证触发器

- **check_prices_inventory**: 确保价格表中的 sold_count 不超过 inventory
- **check_events_attendees**: 确保活动的当前参与人数不超过最大参与人数

## 📈 视图

### events_overview

活动概览视图，包含活动基本信息、商家信息、价格信息等。

### merchant_stats

商家统计视图，包含商家活动数量、发布活动数量、总参与人数、总收入等。

## 🚀 后续步骤

### 1. 创建测试数据

你可以创建一些测试数据来验证功能：

```sql
-- 创建测试商家
INSERT INTO public.merchants (owner_user_id, name, description, contact_email, verified, status)
VALUES (
  (SELECT id FROM public.users WHERE role = 'merchant' LIMIT 1),
  'Test Merchant',
  'Test merchant description',
  'merchant@example.com',
  true,
  'active'
);

-- 创建测试活动
INSERT INTO public.events (merchant_id, title, description, start_at, end_at, venue_name, status)
VALUES (
  (SELECT id FROM public.merchants LIMIT 1),
  'Test Event',
  'Test event description',
  NOW() + INTERVAL '7 days',
  NOW() + INTERVAL '7 days' + INTERVAL '3 hours',
  'Test Venue',
  'published'
);
```

### 2. 配置环境变量

确保以下环境变量已正确配置：

- `SUPABASE_URL`
- `SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- `NEXT_PUBLIC_SITE_URL`

### 3. 测试 API 端点

测试所有 API 端点确保功能正常：

- `POST /api/auth/register` - 用户注册
- `POST /api/auth/login` - 用户登录
- `GET /api/events` - 获取活动列表
- `POST /api/merchant/create` - 创建商家
- `POST /api/orders` - 创建订单

## ❓ 常见问题

### Q: 执行脚本后出现错误怎么办？

A: 检查错误信息，通常是因为：
- 表已存在但结构不同
- 外键约束冲突
- 权限问题

解决方法：
1. 检查错误信息
2. 手动删除有问题的表
3. 重新执行脚本

### Q: 数据没有同步到 public.users？

A: 检查触发器是否已创建：

```sql
-- 检查触发器
SELECT trigger_name, event_manipulation, event_object_table
FROM information_schema.triggers
WHERE event_object_schema = 'auth'
AND event_object_table = 'users';
```

如果触发器不存在，手动创建：

```sql
-- 创建触发器
CREATE TRIGGER on_auth_user_created_to_users
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.handle_new_auth_user_to_users();
```

### Q: RLS 策略导致无法访问数据？

A: 检查 RLS 策略是否正确：

```sql
-- 检查 RLS 策略
SELECT schemaname, tablename, policyname, permissive, roles, cmd, qual
FROM pg_policies
WHERE schemaname = 'public'
ORDER BY tablename, policyname;
```

如果需要临时禁用 RLS 进行调试：

```sql
-- 临时禁用 RLS（仅用于调试）
ALTER TABLE public.users DISABLE ROW LEVEL SECURITY;
```

### Q: 如何回滚？

A: 如果出现问题，可以：

1. **恢复备份**（如果你有备份）
2. **重新执行脚本**（脚本是幂等的，可以安全地多次执行）
3. **手动修复**（根据错误信息手动修复问题）

## 📝 注意事项

1. **不要在生产环境直接执行**（除非你确定要删除所有数据）
2. **先备份数据**（如果需要保留某些数据）
3. **测试环境先验证**（在测试环境先执行，确认无误后再在生产环境执行）
4. **监控错误日志**（执行后检查错误日志，确保没有遗漏的问题）

## 🔗 相关文件

- `supabase/migrations/20251111_complete_database_rebuild.sql` - 完整的数据库重建脚本
- `supabase/migrations/20251111_create_users_table_complete.sql` - 用户表创建脚本
- `FIX_USERS_TABLE.md` - 用户表修复指南
- `问题修复总结.md` - 问题修复总结

## ✅ 验证清单

执行重建后，请验证以下项目：

- [ ] 所有表已创建
- [ ] 所有索引已创建
- [ ] 所有触发器已创建
- [ ] 所有 RLS 策略已设置
- [ ] 用户注册功能正常
- [ ] Google OAuth 登录功能正常
- [ ] 商家创建功能正常
- [ ] 活动创建功能正常
- [ ] 订单创建功能正常
- [ ] 票据生成功能正常
- [ ] 错误日志中没有错误

## 🎉 完成

如果所有验证项目都通过，恭喜！数据库重建完成，你现在有一个干净的、功能完整的数据库。

如果遇到任何问题，请查看错误日志或联系技术支持。

