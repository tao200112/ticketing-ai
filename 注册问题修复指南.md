# 注册问题修复指南

## 问题症状

- 用户注册时出现错误："Database error saving new user"
- Supabase Auth 返回 500 错误
- 错误发生在 `/signup` 端点
- Google OAuth 登录也失败，错误相同

## 根本原因

**触发器函数执行失败**，导致 Supabase Auth 认为注册失败并回滚事务。

可能的原因：
1. **RLS 策略阻止插入**：即使触发器函数使用 `SECURITY DEFINER`，RLS 策略仍然可能阻止插入
2. **触发器函数抛出异常**：即使有异常处理，Supabase 可能仍然检测到错误
3. **权限问题**：触发器函数可能没有足够的权限
4. **约束冲突**：唯一约束或其他约束可能冲突
5. **表结构问题**：表可能不存在或结构不正确

## 解决方案

### 方案 1: 执行完整修复脚本（推荐）

在 Supabase Dashboard 的 SQL Editor 中执行：

```sql
-- 执行 FIX_REGISTRATION_ISSUE.sql
```

这个脚本会：
1. 确保 users 表存在并正确配置
2. 重新创建触发器函数（最简化版本）
3. 配置 RLS 策略（允许触发器插入）
4. 设置正确的权限
5. 同步现有数据
6. 验证设置

### 方案 2: 分步执行

如果完整脚本有问题，可以分步执行：

#### 步骤 1: 修复触发器函数

执行 `fix_trigger_robust.sql`：
- 创建最健壮的触发器函数
- 使用多重异常处理
- 确保不会抛出异常

#### 步骤 2: 修复 RLS 策略

执行 `fix_rls_for_trigger.sql`：
- 创建允许触发器插入的策略
- 确保服务角色可以执行所有操作

#### 步骤 3: 验证设置

执行 `check_trigger_issue.sql`：
- 检查触发器状态
- 测试插入操作
- 查看错误日志

### 方案 3: 临时禁用触发器（仅用于测试）

如果问题仍然存在，可以临时禁用触发器来测试：

```sql
-- 临时禁用触发器
DROP TRIGGER IF EXISTS on_auth_user_created_to_users ON auth.users;

-- 测试注册
-- 如果注册成功，说明问题确实在触发器

-- 然后重新创建触发器
-- 使用 fix_trigger_robust.sql 中的简化版本
```

## 关键修复点

### 1. 触发器函数异常处理

**问题**：即使有异常处理，Supabase 仍然可能检测到错误。

**解决方案**：
- 使用 `RAISE WARNING` 而不是 `RAISE EXCEPTION`
- 使用嵌套的 `BEGIN...EXCEPTION...END` 块
- 确保在所有情况下都返回 `NEW`

### 2. RLS 策略

**问题**：RLS 策略可能阻止触发器插入。

**解决方案**：
- 创建允许触发器插入的策略
- 使用 `SECURITY DEFINER` 函数
- 确保策略允许服务角色和 postgres 用户操作

### 3. 权限设置

**问题**：权限可能不正确。

**解决方案**：
- 确保函数使用 `SECURITY DEFINER`
- 授予所有必要的权限
- 确保表有正确的权限

## 执行步骤

### 1. 在 Supabase Dashboard 中执行修复脚本

1. 打开 Supabase Dashboard
2. 进入 SQL Editor
3. 执行 `FIX_REGISTRATION_ISSUE.sql`
4. 检查执行结果
5. 查看是否有错误

### 2. 验证设置

执行 `check_trigger_issue.sql` 来验证：
- 触发器是否正确设置
- RLS 策略是否正确
- 表结构是否正确

### 3. 测试注册

1. 尝试注册新用户
2. 检查 Supabase 日志
3. 验证用户是否创建成功
4. 检查 `public.users` 表是否有新记录

### 4. 如果问题仍然存在

1. **检查 Supabase 日志**：
   - 查看详细的错误信息
   - 查找触发器函数的错误

2. **检查 PostgreSQL 日志**：
   - 查看数据库日志
   - 查找约束冲突或其他错误

3. **测试触发器函数**：
   - 手动执行触发器函数
   - 检查是否有错误

4. **检查环境变量**：
   - 确保 Supabase 环境变量正确设置
   - 检查 API 密钥是否正确

## 常见问题

### Q: 为什么触发器函数失败会导致注册失败？

A: Supabase Auth 在创建用户后会触发触发器。如果触发器抛出异常，Supabase 可能会回滚整个事务，导致注册失败。

### Q: 如何确保触发器不会导致注册失败？

A: 在触发器函数的异常处理中，使用 `RAISE WARNING` 而不是 `RAISE EXCEPTION`，并确保返回 `NEW` 以允许 Supabase Auth 继续。

### Q: RLS 策略会影响触发器吗？

A: 触发器函数使用 `SECURITY DEFINER`，所以它可以绕过 RLS。但为了安全，我们还是需要正确的 RLS 策略。

### Q: 如果修复后问题仍然存在怎么办？

A: 
1. 检查 Supabase 日志获取详细错误信息
2. 执行 `check_trigger_issue.sql` 诊断问题
3. 临时禁用触发器测试注册
4. 检查表结构和约束

## 验证清单

- [ ] users 表存在
- [ ] 触发器函数存在
- [ ] 触发器存在
- [ ] RLS 已启用
- [ ] RLS 策略正确
- [ ] 权限设置正确
- [ ] 测试注册成功
- [ ] 用户数据同步到 public.users 表

## 下一步

1. 执行 `FIX_REGISTRATION_ISSUE.sql`
2. 验证设置
3. 测试注册功能
4. 检查日志
5. 如果问题仍然存在，查看详细的错误信息

