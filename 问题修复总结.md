# 注册和登录功能报错修复总结

## 问题描述

生产环境出现以下错误：
1. **注册功能报错**：`POST /api/auth/register` 返回 500 错误，错误信息 "Database query failed"
2. **Google 登录报错**：`POST /api/auth/login-from-supabase` 返回 500 错误，错误信息 "Could not find the table 'public.users' in the schema cache"

## 问题原因

**根本原因**：生产环境的 Supabase 数据库中缺少 `public.users` 表。

当用户注册或通过 Google OAuth 登录时：
1. Supabase Auth 会在 `auth.users` 表中创建用户记录
2. 触发器应该自动将用户同步到 `public.users` 表
3. 但是 `public.users` 表不存在，导致触发器失败
4. 应用程序代码尝试查询 `public.users` 表时也失败

## 解决方案

### 步骤 1: 执行数据库迁移

在 Supabase Dashboard 的 SQL Editor 中执行以下脚本之一：

**选项 A：完整迁移脚本（推荐）**
- 文件：`supabase/migrations/20251111_create_users_table_complete.sql`
- 包含完整的表结构、索引、触发器、RLS 策略等

**选项 B：快速修复脚本**
- 文件：`QUICK_FIX_USERS_TABLE.sql`
- 简化版本，快速修复问题

### 步骤 2: 验证修复

执行以下 SQL 查询验证表是否创建成功：

```sql
-- 检查表是否存在
SELECT EXISTS (
  SELECT FROM information_schema.tables 
  WHERE table_schema = 'public' 
  AND table_name = 'users'
);

-- 检查表结构
SELECT column_name, data_type, is_nullable
FROM information_schema.columns
WHERE table_schema = 'public'
AND table_name = 'users'
ORDER BY ordinal_position;

-- 检查数据同步
SELECT 
  'auth.users' as table_name,
  COUNT(*) as count
FROM auth.users
UNION ALL
SELECT 
  'public.users' as table_name,
  COUNT(*) as count
FROM public.users;
```

### 步骤 3: 测试功能

1. **测试邮箱注册**
   - 访问注册页面
   - 填写注册表单
   - 确认注册成功

2. **测试 Google OAuth 登录**
   - 点击 Google 登录按钮
   - 完成 OAuth 授权
   - 确认登录成功

3. **检查错误日志**
   - 查看 Vercel 日志
   - 确认不再出现 `public.users` 表缺失的错误

## 迁移脚本功能

迁移脚本会执行以下操作：

1. **创建 users 表**
   - 包含所有必需的列
   - 设置唯一约束：`(email, role)`
   - 设置外键约束：`id` 引用 `auth.users(id)`

2. **创建索引**
   - `idx_users_email` - 邮箱索引
   - `idx_users_role` - 角色索引
   - `idx_users_auth_provider` - 认证提供商索引

3. **创建触发器**
   - `on_auth_user_created_to_users` - 自动同步新用户
   - `handle_new_auth_user_to_users()` - 触发器函数

4. **设置 RLS 策略**
   - 允许服务角色完全管理用户
   - 允许用户查看和更新自己的资料
   - 允许公开注册（插入新用户）
   - 允许认证用户读取用户数据

5. **回填历史数据**
   - 将现有的 `auth.users` 数据同步到 `public.users`

## 注意事项

1. **幂等性**：迁移脚本是幂等的，可以安全地多次执行
2. **数据安全**：迁移脚本使用 `ON CONFLICT` 处理冲突，不会丢失数据
3. **自动同步**：触发器会在新用户注册时自动同步数据
4. **RLS 策略**：确保数据安全，只有授权用户才能访问数据

## 相关文件

- `supabase/migrations/20251111_create_users_table_complete.sql` - 完整的迁移脚本
- `QUICK_FIX_USERS_TABLE.sql` - 快速修复脚本
- `FIX_USERS_TABLE.md` - 详细的修复指南
- `supabase/migrations/202511080345_handle_new_auth_user_upsert.sql` - 用户同步触发器

## 后续建议

1. **定期检查数据库迁移**
   - 确保所有迁移脚本都已执行
   - 使用 Supabase CLI 管理迁移

2. **监控错误日志**
   - 设置错误监控
   - 及时发现类似问题

3. **测试环境验证**
   - 在测试环境先执行迁移
   - 验证功能正常后再部署到生产环境

4. **文档更新**
   - 更新部署文档
   - 记录数据库迁移步骤

## 如果问题仍然存在

如果执行迁移后问题仍然存在，请检查：

1. **Supabase 项目配置**
   - 确认使用的是正确的 Supabase 项目
   - 检查环境变量是否正确设置

2. **API 路由**
   - 检查 `/api/auth/register` 和 `/api/auth/login-from-supabase` 路由
   - 确认这些路由不再被使用（应该使用 Supabase Auth 直接）

3. **前端代码**
   - 确认前端使用 Supabase Auth 进行注册和登录
   - 检查 `lib/auth-context.js` 是否正确配置

4. **错误日志**
   - 查看详细的错误日志
   - 确认错误是否来自其他地方

